// Create a function that takes an object argument sizes (contains width, length, height keys)
// and returns the volume of the box.
// https://edabit.com/challenge/ms3q5GYSpFpwxeFWX

const volumeOfBox = ({ length, width, height }) => length * width * height;

module.exports = volumeOfBox;
